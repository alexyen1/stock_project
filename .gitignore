# Secrets
.env

# Database — Postgres is now the source of truth; keep the local SQLite out of git.
data/*.db
data/*.sqlite
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Streamlit local secrets — contains DATABASE_URL, never commit.
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store
.vscode/
.idea/
